import { ControlErrorMessagesPipe } from './control-error-messages.pipe';

describe('ControlErrorMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlErrorMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
